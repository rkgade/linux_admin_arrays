#include <stdio.h>

int main(void) {

	
	int i,sum=0,array[5];
	for(i=0;i<5;i++)
	{
			
			scanf("%d",&array[i]);
	}
	for(i=0;i<5;i++)
	{
		sum=sum+array[i];
		
	}
	printf("Sum is: %d",sum);
	return 0;
}
